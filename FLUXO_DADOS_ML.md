# 🔄 FLUXO DE DADOS - INTEGRAÇÕES MERCADO LIVRE

Este documento mostra o caminho dos dados desde o frontend até a API do Mercado Livre, identificando onde cada problema ocorre.

---

## 📊 FLUXO 1: AUTENTICAÇÃO INICIAL (OAuth)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ FRONTEND (concorrentes.html, dashboard.html)                            │
│                                                                          │
│ 1. Usuário clica "Conectar Mercado Livre"                               │
│    └─> authFetch('/api/ml-auth-url')                                    │
│        └─> Header: Authorization: Bearer {JWT_CLERK}                    │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:507-530)                                               │
│                                                                          │
│ 2. Rota GET /api/ml-auth-url                                            │
│    └─> Valida JWT Clerk (get_current_user)                              │
│    └─> Chama get_auth_url()                                             │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ SERVIÇO ML (services/ml_api.py:16-27)                                   │
│                                                                          │
│ 3. get_auth_url()                                                        │
│    ┌─> Verifica ML_APP_ID ❌ AUSENTE                                    │
│    └─> Verifica ML_REDIRECT_URI ❌ AUSENTE                              │
│         └─> return None (linha 19) 🚨 PROBLEMA CRÍTICO                  │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:527-529)                                               │
│                                                                          │
│ 4. if url is None:                                                       │
│    └─> HTTPException(503, "ML não configurado")                         │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ FRONTEND                                                                 │
│                                                                          │
│ 5. Recebe erro 503                                                       │
│    └─> Alert: "ML não configurado"                                      │
└─────────────────────────────────────────────────────────────────────────┘
```

**🔧 CORREÇÃO:** Adicionar `ML_APP_ID`, `ML_SECRET`, `ML_REDIRECT_URI` no `.env`

---

## 📊 FLUXO 2: BUSCA DE CONCORRENTES (Search Public)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ FRONTEND (concorrentes.html:331-423)                                    │
│                                                                          │
│ 1. Usuário digita termo de busca e clica "Buscar"                       │
│    └─> authFetch('/api/ml/search?q=...&limit=20')                       │
│        └─> Header: Authorization: Bearer {JWT_CLERK}                    │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:722-745)                                               │
│                                                                          │
│ 2. Rota GET /api/ml/search                                              │
│    ├─> Valida JWT Clerk (paid_guard)                                    │
│    └─> Chama search_public(site_id="MLB", q=..., access_token=None)     │
│        (linha 734: tenta SEM token primeiro)                            │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ SERVIÇO ML (services/ml_api.py:159-192)                                 │
│                                                                          │
│ 3. search_public()                                                       │
│    └─> requests.get("https://api.mercadolibre.com/sites/MLB/search")    │
│        ├─> Params: q=..., limit=20                                      │
│        └─> Headers: User-Agent (sem Authorization, pois token=None)     │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ API MERCADO LIVRE                                                        │
│                                                                          │
│ 4. Responde com:                                                         │
│    ├─> 200 OK (se app é certificado ou busca pública permitida)         │
│    └─> 403 Forbidden (se app NÃO é certificado) 🚨 ESPERADO             │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ SERVIÇO ML (services/ml_api.py:186-192)                                 │
│                                                                          │
│ 5. if resp.status_code != 200:                                          │
│    └─> _log.warning(...) 🚨 PROBLEMA: Apenas warning, não exceção       │
│    └─> return None                                                      │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:735-744)                                               │
│                                                                          │
│ 6. if result is None:                                                    │
│    ├─> Tenta novamente COM token (get_valid_ml_token)                   │
│    │   (mas também falhará se ML_APP_ID ausente)                        │
│    └─> if result is None:                                               │
│         └─> HTTPException(503, "Busca restrita")                        │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ FRONTEND (concorrentes.html:355-365)                                    │
│                                                                          │
│ 7. if (res.status === 503):                                             │
│    └─> Mostra mensagem: "Busca restrita. Use adicionar por link/ID"    │
└─────────────────────────────────────────────────────────────────────────┘
```

**🔧 CORREÇÃO:**
1. Adicionar credenciais ML no `.env` (problema #1)
2. Lançar exceção com detalhes em vez de retornar `None` (problema #2)

---

## 📊 FLUXO 3: ADICIONAR CONCORRENTE POR LINK/ID

```
┌─────────────────────────────────────────────────────────────────────────┐
│ FRONTEND (concorrentes.html:296-329)                                    │
│                                                                          │
│ 1. Usuário cola link ou ID (ex: MLB1234567890)                          │
│    └─> authFetch('/api/ml/competitors', {method: 'POST', body: ...})    │
│        └─> Header: Authorization: Bearer {JWT_CLERK}                    │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:766-790)                                               │
│                                                                          │
│ 2. Rota POST /api/ml/competitors                                        │
│    ├─> Valida JWT Clerk (paid_guard)                                    │
│    ├─> Chama get_valid_ml_token(user) ❌ Pode retornar None             │
│    ├─> if not token: HTTPException(403, "ml_not_connected")             │
│    ├─> Extrai item_id com _parse_ml_item_id()                           │
│    └─> Chama get_item_by_id(token.access_token, item_id)                │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ SERVIÇO ML (services/ml_api.py:289-311)                                 │
│                                                                          │
│ 3. get_item_by_id()                                                      │
│    └─> requests.get(f"https://api.mercadolibre.com/items/{item_id}")    │
│        ├─> Headers: Authorization: Bearer {access_token}                │
│        └─> User-Agent: MLIntelligence/1.0                               │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ API MERCADO LIVRE                                                        │
│                                                                          │
│ 4. Responde com:                                                         │
│    ├─> 200 OK + dados do item (se token válido E item existe)           │
│    ├─> 403 Forbidden (se token expirado/inválido)                       │
│    └─> 404 Not Found (se item não existe)                               │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ SERVIÇO ML (services/ml_api.py:298-308)                                 │
│                                                                          │
│ 5. if status_code == 200: return data                                   │
│    if status_code == 403 and access_token:                              │
│    ├─> Tenta novamente SEM token (itens públicos)                       │
│    │   └─> if 200: return data                                          │
│    └─> _log.warning(...) + return None                                  │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:775-777)                                               │
│                                                                          │
│ 6. if not item:                                                          │
│    └─> HTTPException(404, "Anúncio não encontrado")                     │
│    else:                                                                 │
│    └─> Salva no banco (CompetitorItem)                                  │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ FRONTEND (concorrentes.html:315-324)                                    │
│                                                                          │
│ 7. if (!res.ok): mostra erro                                            │
│    else: mostra sucesso + recarrega lista                               │
└─────────────────────────────────────────────────────────────────────────┘
```

**🔧 CORREÇÃO:**
1. Problema #1: Token pode ser `None` se `ML_APP_ID/ML_SECRET` ausentes
2. Problema #4: Token expirado não notifica usuário corretamente

---

## 📊 FLUXO 4: PERGUNTAS NOS ANÚNCIOS (Webhook)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ API MERCADO LIVRE                                                        │
│                                                                          │
│ 1. Comprador faz pergunta em anúncio                                    │
│    └─> ML envia POST /api/ml-webhook                                    │
│        ├─> Body: {topic: "questions", resource: "/questions/123", ...}  │
│        └─> Header: x-signature (HMAC para validação, se configurado)    │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:1035-1089)                                             │
│                                                                          │
│ 2. Rota POST /api/ml-webhook                                            │
│    ├─> Valida topic == "questions"                                      │
│    ├─> Extrai question_id e seller_id do body                           │
│    ├─> Busca usuário por seller_id                                      │
│    │   └─> if not user: _log.warning(...) + return 200 🚨 PROBLEMA #7   │
│    └─> if user: add_task(_process_ml_question_webhook)                  │
│         └─> return 200 OK (em <500ms)                                   │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (background task)                                                │
│                                                                          │
│ 3. _process_ml_question_webhook(question_id, user_id)                   │
│    ├─> Busca token ML do usuário                                        │
│    ├─> Chama get_question_detail(token, question_id)                    │
│    ├─> Gera resposta IA (LLM)                                           │
│    └─> Salva em PendingQuestion (status="pending")                      │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ FRONTEND (perguntas-anuncios.html:134-191)                              │
│                                                                          │
│ 4. Usuário abre página de perguntas                                     │
│    └─> authFetch('/api/ml/questions/pending')                           │
│        └─> Mostra lista de perguntas aguardando aprovação               │
└─────────────────────────────────────────────────────────────────────────┘
```

**🔧 CORREÇÃO:**
1. Problema #7: Retornar 404 se usuário não encontrado (para ML reenviar)
2. Problema #1: Token pode ser `None` se credenciais ausentes

---

## 📊 FLUXO 5: RENOVAÇÃO AUTOMÁTICA DE TOKEN

```
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:566-594)                                               │
│                                                                          │
│ 1. Qualquer rota chama get_valid_ml_token(user)                         │
│    └─> Busca token no banco (MlToken)                                   │
│    └─> Verifica expires_at                                              │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:576)                                                    │
│                                                                          │
│ 2. if expires_at <= now + 5min:                                          │
│    └─> Token expirado ou próximo de expirar                             │
│    └─> Chama refresh_access_token(refresh_token)                        │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ SERVIÇO ML (services/ml_api.py:47-60)                                   │
│                                                                          │
│ 3. refresh_access_token()                                                │
│    ┌─> Verifica ML_APP_ID ❌ AUSENTE                                    │
│    └─> Verifica ML_SECRET ❌ AUSENTE                                    │
│         └─> return None (linha 50) 🚨 PROBLEMA CRÍTICO                  │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (main.py:588-590)                                               │
│                                                                          │
│ 4. if new_tokens and "access_token" in new_tokens:                      │
│    └─> Atualiza token no banco                                          │
│    else:                                                                 │
│    └─> logger.warning(...) + return None 🚨 PROBLEMA #4                 │
└────────────────────────────────┬────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ BACKEND (qualquer rota que chamou get_valid_ml_token)                   │
│                                                                          │
│ 5. if not token:                                                         │
│    └─> HTTPException(403, "ml_not_connected")                           │
│         └─> Usuário não sabe que token expirou                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**🔧 CORREÇÃO:**
1. Problema #1: Adicionar `ML_SECRET` no `.env`
2. Problema #4: Retornar código de erro específico (ex: `token_expired`) em vez de genérico `ml_not_connected`

---

## 🎯 PONTOS DE FALHA IDENTIFICADOS

| Ponto | Arquivo | Linha | Problema |
|-------|---------|-------|----------|
| A | `services/ml_api.py` | 18-19 | `ML_APP_ID` ausente → `get_auth_url()` retorna `None` |
| B | `services/ml_api.py` | 49-50 | `ML_SECRET` ausente → `refresh_access_token()` retorna `None` |
| C | `services/ml_api.py` | 186-192 | Erro de API ML retorna `None` (silencioso) |
| D | `main.py` | 588-590 | Falha na renovação retorna `None` sem detalhes |
| E | `main.py` | 1086-1088 | Webhook retorna 200 mesmo sem processar (pergunta perdida) |
| F | `concorrentes.html` | 367-370 | Erro não é logado no console (dificulta debug) |

---

## ✅ FLUXO CORRETO (APÓS CORREÇÕES)

```
1. Adicionar ML_APP_ID, ML_SECRET, ML_REDIRECT_URI no .env
   └─> Reiniciar backend

2. Usuário clica "Conectar Mercado Livre"
   └─> get_auth_url() retorna URL válida ✅
   └─> Redireciona para autorização ML ✅
   └─> Callback salva tokens no banco ✅

3. Usuário busca concorrentes
   └─> Token válido é obtido ✅
   └─> Requisição para API ML com Authorization header ✅
   └─> Dados retornam (ou 403 se app não certificado - esperado) ✅

4. Token expira após 6 horas
   └─> get_valid_ml_token() detecta expiração ✅
   └─> refresh_access_token() renova com ML_SECRET ✅
   └─> Novo token salvo no banco ✅
   └─> Usuário continua usando normalmente ✅
```

---

**Próximo passo:** Implementar correções conforme `RELATORIO_BUGS.md` e testar cada fluxo acima.
